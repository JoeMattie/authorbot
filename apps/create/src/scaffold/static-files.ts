/**
 * Files the scaffold copies out of `templates/book-repo` unchanged.
 *
 * They are embedded rather than read from disk because `@authorbot/create`
 * ships as an npm package containing only `dist/` (ADR-0022) — the templates
 * directory is not published, so a runtime read would work in this repository
 * and fail for every real user.
 *
 * GENERATED. Do not hand-edit: `test/template-drift.test.ts` asserts every
 * entry is byte-identical to its counterpart under `templates/book-repo`, so
 * an edit here without the matching edit there fails the suite, and the
 * template stays the single source of truth.
 *
 * Regenerate with: node apps/create/scripts/sync-templates.mjs
 */
export const STATIC_TEMPLATE_FILES: Readonly<Record<string, string>> = {
  ".gitignore": "# Build output: `authorbot build . --out _site` regenerates it from the\n# Markdown and YAML that IS the book. Committing it would mean reviewing a\n# rendered copy of every change alongside the change itself.\n_site/\n\n# Wizard progress journal (Phase 6): resumable setup state, never secrets.\n.authorbot-setup.json\n\n# Local-only wrangler values. Real deployment secrets are set with\n# `wrangler secret put` and never live in the repository.\n.dev.vars\n\nnode_modules/\n.DS_Store\n",
  "story/outline.yml": "# Story graph (schema: authorbot.story-graph/v1).\n#\n# Method-neutral planning nodes: premise, arc, part, chapter, scene, beat,\n# custom. This template ships EMPTY — a blank book has nothing to outline yet,\n# and an outline referencing chapters that do not exist fails validation.\n#\n# Add nodes as the story takes shape, for example:\n#\n#   nodes:\n#     - id: premise:main\n#       type: premise\n#       title: Core premise\n#       summary: One sentence describing what the book is about.\n#       order: 10\n#     - id: chapter:opening\n#       type: chapter\n#       title: Opening\n#       chapter_id: <the chapter's UUIDv7, from its frontmatter `id`>\n#       parent: premise:main\n#       order: 20\n#       status: draft\n#\n# `chapter_id` must match a real chapter's `id`, and `parent` must name a node\n# defined in this file — `authorbot validate` checks both.\n#\n# A filled-in example lives in examples/book-repo/story/outline.yml.\nschema: authorbot.story-graph/v1\nnodes: []\n",
  "story/timeline.yml": "# Timeline (schema: authorbot.timeline/v1).\n#\n# `sort_key` is the sortable value; `display_time` is the human label. This\n# keeps custom calendars and relative time sortable without parsing labels.\n#\n# This template ships EMPTY. The calendar below is a neutral default: story\n# time is measured relative to whatever you decide the beginning is. Change\n# `type` to `absolute` for real-world dates, or to a label of your own for a\n# custom calendar — nothing parses this value; it tells collaborators and\n# agents how to read `display_time`.\n#\n# Add events as the story takes shape, for example:\n#\n#   events:\n#     - id: event:opening-event\n#       sort_key: 100\n#       display_time: Day 0\n#       title: The story begins\n#       participants:\n#         - character:protagonist\n#       chapter_refs:\n#         - <the chapter's UUIDv7, from its frontmatter `id`>\n#       facts:\n#         - Continuity facts live here as short declarative sentences.\n#\n# `participants` must name characters that exist in story/characters/, and\n# `chapter_refs` must name real chapter IDs — `authorbot validate` checks both.\n#\n# A filled-in example lives in examples/book-repo/story/timeline.yml.\nschema: authorbot.timeline/v1\ncalendar:\n  type: relative\nevents: []\n",
  ".github/workflows/validate.yml": "# Authorbot validation workflow (design §17.1).\n#\n# Runs on pull requests and pushes that touch book content or collaboration\n# records and fails on any validation error. Annotation traffic runs\n# validation only — it never rebuilds the book (design §7.4); publication is\n# publish.yml's job.\n#\n# What validation covers:\n#   - book.yml and all frontmatter schemas\n#   - unique chapter IDs, slugs, order values, and chapter routes\n#   - block ID presence, uniqueness, and UUIDv7 format\n#   - annotation selectors and cross-references (story graph, timeline,\n#     characters, work items, decisions, releases, attribution)\n#   - raw HTML and unsafe URL schemes in prose\n#   - work-item original-text delimiter integrity\n#\n# After validating, the site is built to a throwaway directory (design\n# §17.1: the validation workflow also builds the site) so a change that\n# validates but cannot build — or would emit colliding routes — fails here,\n# on the pull request, instead of on main after merge. Nothing is deployed;\n# link and accessibility checks over real output run in the Authorbot\n# toolchain's own e2e suite.\n#\n# Nothing is deployed from here under any circumstance. Deployment is\n# publish.yml's job, and its only target is Cloudflare (ADR-0020).\n#\n# NO CLONE, NO COMPILE (ADR-0022). Like publish.yml, this installs the\n# published `@authorbot/cli` package from package-lock.json and runs its\n# binary. Because both workflows install from that same lockfile, the\n# toolchain that validates a pull request is by construction the toolchain\n# that will publish it — they cannot drift, and there is no AUTHORBOT_REF\n# repository variable to keep in sync any more.\n#\n# If the book is served under a base path, the AUTHORBOT_BASE_URL repository\n# variable is read here too, so the smoke build exercises the same prefixing\n# publish.yml will apply (ADR-0019 §6).\n\nname: validate\n\non:\n  # Manual re-run (e.g. after a GitHub Actions incident).\n  workflow_dispatch: {}\n  push:\n    paths:\n      - \"book.yml\"\n      - \"chapters/**\"\n      - \"story/**\"\n      - \".authorbot/**\"\n      - \"package.json\"\n      - \"package-lock.json\"\n  pull_request:\n    paths:\n      - \"book.yml\"\n      - \"chapters/**\"\n      - \"story/**\"\n      - \".authorbot/**\"\n      - \"package.json\"\n      - \"package-lock.json\"\n\npermissions:\n  contents: read\n\njobs:\n  validate:\n    runs-on: ubuntu-latest\n    steps:\n      - name: Check out the book repository\n        uses: actions/checkout@v4\n\n      - name: Set up Node\n        uses: actions/setup-node@v4\n        with:\n          node-version: 22\n          cache: npm\n\n      - name: Install the pinned Authorbot toolchain\n        run: |\n          if [ ! -f package-lock.json ]; then\n            echo \"::error::package-lock.json is missing. Run 'npm install' locally and commit the lockfile — it is what pins your toolchain version.\"\n            exit 1\n          fi\n          npm ci --no-audit --no-fund\n\n      - name: Validate the book repository\n        run: npx authorbot validate .\n\n      # Build smoke test (design §17.1): catches anything that validates but\n      # cannot publish before it lands on main. Nothing is deployed. The\n      # --base-url handling mirrors publish.yml exactly so the two builds\n      # cannot disagree.\n      - name: Build the site (dry run)\n        env:\n          BASE_URL: ${{ vars.AUTHORBOT_BASE_URL }}\n        run: |\n          if [ -n \"$BASE_URL\" ]; then\n            npx authorbot build . --out \"$RUNNER_TEMP/site\" --base-url \"$BASE_URL\"\n          else\n            npx authorbot build . --out \"$RUNNER_TEMP/site\"\n          fi\n",
  ".github/workflows/publish.yml": "# Authorbot publication workflow (design §17.2, ADR-0020, ADR-0021, ADR-0022).\n#\n# Publishes the book as a static reading site on a Cloudflare Worker whenever\n# public content changes on main. It never runs on raw collaboration traffic\n# (design §7.4): annotations and votes are validated by validate.yml without\n# rebuilding the book.\n#\n# NO CLONE, NO COMPILE (ADR-0022). This workflow installs the published\n# `@authorbot/cli` package and runs its binary. It does not check out the\n# Authorbot repository and does not build TypeScript. The version of the\n# toolchain that runs is the one written in this repository's package.json and\n# locked by package-lock.json — visible in your history, diffable in a pull\n# request, and revertable with `git revert` alongside whatever content it was\n# paired with. There is no AUTHORBOT_REF repository variable any more.\n#\n# ONE HOST, BY DESIGN (ADR-0020). GitHub holds the repository; Cloudflare\n# serves the site — and, later, the collaboration API from that same origin\n# (ADR-0019). There is no GitHub Pages path: a static-only host can serve the\n# prose but never the annotations, votes, or work queue, so shipping both\n# produced two live URLs where one silently lacked every collaboration\n# feature. Cloudflare's free tier covers a book of this size.\n#\n# One-time setup for your repository:\n#   1. Edit wrangler.jsonc: set `name` to the Worker name you want.\n#   2. Run `npm install` once locally and COMMIT package-lock.json. `npm ci`\n#      below refuses to run without it, and that is deliberate — an install\n#      without a lockfile is an unpinned install.\n#   3. Create a Cloudflare API token with the \"Edit Cloudflare Workers\"\n#      template and store it as the CLOUDFLARE_API_TOKEN repository *secret*;\n#      store your account id as the CLOUDFLARE_ACCOUNT_ID repository secret.\n#   4. Only if the book is served under a sub-path of a larger site (e.g.\n#      https://example.com/my-book/), set the AUTHORBOT_BASE_URL repository\n#      variable to that path or URL. Leave it unset for a book at the root of\n#      its own hostname, which is the usual case.\n#   5. Only once you have turned on collaboration (stage 3 of\n#      docs/getting-started.md), set the AUTHORBOT_D1_DATABASE repository\n#      variable to your D1 database name. That switches on the migration step\n#      below. Leave it unset for a static-only book — there is no database to\n#      migrate.\n\nname: publish\n\non:\n  push:\n    branches: [main]\n    # Path filters per design §7.4: chapters, story data, book.yml, and\n    # publisher configuration trigger a public rebuild. Releases are included\n    # so a manual release commit republishes; other .authorbot/** records do\n    # not trigger publication. wrangler.jsonc is included because it decides\n    # what is deployed and where, and package-lock.json because it decides\n    # which toolchain builds it.\n    paths:\n      - \"book.yml\"\n      - \"chapters/**\"\n      - \"story/**\"\n      - \".authorbot/releases/**\"\n      - \"wrangler.jsonc\"\n      - \"package.json\"\n      - \"package-lock.json\"\n      - \".github/workflows/publish.yml\"\n  # Manual release / redeploy.\n  workflow_dispatch: {}\n\n# Deploying needs nothing beyond reading this repository: the Cloudflare\n# credentials are repository secrets, not GitHub OIDC scopes.\npermissions:\n  contents: read\n\n# One deployment at a time; let a queued deploy of a newer commit finish\n# rather than cancelling it mid-flight.\nconcurrency:\n  group: publish\n  cancel-in-progress: false\n\njobs:\n  publish:\n    runs-on: ubuntu-latest\n    steps:\n      - name: Check out the book repository\n        uses: actions/checkout@v4\n\n      - name: Set up Node\n        uses: actions/setup-node@v4\n        with:\n          node-version: 22\n          cache: npm\n\n      # `npm ci` installs exactly what package-lock.json records, with\n      # integrity hashes, and fails rather than silently resolving something\n      # newer. The check in front of it turns npm's confusing \"can only\n      # install with an existing package-lock.json\" error into an instruction.\n      - name: Install the pinned Authorbot toolchain\n        run: |\n          if [ ! -f package-lock.json ]; then\n            echo \"::error::package-lock.json is missing. Run 'npm install' locally and commit the lockfile — it is what pins your toolchain version.\"\n            exit 1\n          fi\n          npm ci --no-audit --no-fund\n\n      - name: Validate the book repository\n        run: npx authorbot validate .\n\n      # --base-url survives ADR-0020 for exactly one job: serving the book\n      # under a base path (ADR-0019 §6). A book at the root of its own\n      # hostname needs no prefix, so the flag is passed only when\n      # AUTHORBOT_BASE_URL is set — an empty --base-url is a usage error, and\n      # a wrong one produces a site whose internal links and stylesheet 404.\n      - name: Build the static site and build manifest\n        env:\n          BASE_URL: ${{ vars.AUTHORBOT_BASE_URL }}\n        run: |\n          if [ -n \"$BASE_URL\" ]; then\n            npx authorbot build . --out _site --base-url \"$BASE_URL\"\n          else\n            npx authorbot build . --out _site\n          fi\n\n      # ADR-0021 §4 — MIGRATIONS RUN BEFORE THE DEPLOY, NOT AFTER.\n      #\n      # The Worker currently serving your readers keeps serving throughout the\n      # deploy, so for the seconds between these two steps the OLD code is\n      # talking to the NEW schema. Migrating first is the only ordering that\n      # is safe in that window: a migration can be written to be compatible\n      # with the code already running, whereas a Worker cannot be written to\n      # tolerate columns that do not exist yet. Deploy-then-migrate leaves a\n      # live Worker querying a table it has not got.\n      #\n      # That safety is conditional on migrations being EXPAND/CONTRACT: only\n      # additive changes ship in a release that also changes the Worker.\n      # Removing a column or tightening a constraint takes two releases —\n      # expand in one, deploy, contract in the next — so no deployed version\n      # ever meets a schema it was not written for. Authorbot's own migrations\n      # follow this rule; if you write your own, it applies to you too.\n      #\n      # The SQL comes from node_modules/@authorbot/api/migrations, named as\n      # `migrations_dir` in wrangler.jsonc, so the schema and the Worker code\n      # are always the same pinned version. Applying is idempotent: wrangler\n      # records what it has run in a d1_migrations table and skips it next\n      # time. Skipped entirely for a static-only book, which has no database.\n      - name: Apply pending database migrations\n        if: ${{ vars.AUTHORBOT_D1_DATABASE != '' }}\n        env:\n          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}\n          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}\n          # Passed as an environment variable rather than interpolated into\n          # the command: an expression is substituted into the shell script\n          # before the shell sees it, so a value containing shell syntax would\n          # execute rather than be an argument.\n          D1_DATABASE: ${{ vars.AUTHORBOT_D1_DATABASE }}\n        run: npx wrangler d1 migrations apply \"$D1_DATABASE\" --remote\n\n      # Deploys the Worker described by wrangler.jsonc, whose `assets` block\n      # points at the _site directory built above. Stage 3 of\n      # docs/getting-started.md upgrades that same Worker to also serve the\n      # collaboration API from this one origin; this step does not change\n      # when that happens.\n      - name: Deploy to Cloudflare\n        uses: cloudflare/wrangler-action@v3\n        with:\n          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}\n          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}\n",
};

/** Directories the template keeps with a `.gitkeep` (contract §3.2 scaffold). */
export const KEEP_DIRECTORIES: readonly string[] = [
  "chapters",
  "story/characters",
  ".authorbot/annotations",
  ".authorbot/attribution",
  ".authorbot/decisions",
  ".authorbot/exports",
  ".authorbot/releases",
  ".authorbot/work-items",
];
