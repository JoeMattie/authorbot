# work-item-missing-target

A schema-valid revise_range work item with no chapter_id, base_revision, or source_annotation_id: it has no target document (design section 9.1), so the per-type reference requirements are unmet.
